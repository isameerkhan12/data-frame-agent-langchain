# Core data manipulation
pandas>=2.0.0

# LangChain base package – provides AgentExecutor, memory, prompt templates
langchain>=0.2.0

# LangChain classic compatibility package – includes ConversationBufferMemory
langchain-classic>=0.3.0

# LangChain community integrations – includes ChatOllama
langchain-community>=0.2.0

# LangChain experimental – provides create_pandas_dataframe_agent
langchain-experimental>=0.0.60

# HTTP library used internally by langchain-community for Ollama requests
requests>=2.31.0

# Required by pandas.to_markdown(), used by create_pandas_dataframe_agent
tabulate>=0.9.0

# LangGraph – Graph-based agent framework for complex workflows and reflexion
# Enables the Reflexion pattern with explicit nodes and state machine
langgraph>=0.0.30

# Python typing extensions for advanced type hints (TypedDict, Annotated)
# Required by LangGraph for state type definitions
typing-extensions>=4.8.0
